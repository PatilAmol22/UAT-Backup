({
	/*getProductCategory : function(component, event, helper) {
    console.log('rreached in rebate details helper 1');
    var action = component.get("c.getProductCategory");
        //Set the Object parameters and Field Set name
        
        action.setCallback(this, function(response){
            var state = response.getState();
        console.log('rreached in rebate details helper 2');    
            if(state === 'SUCCESS'){
                
                component.set("v.ProductCategory", response.getReturnValue());
                console.log(''+ component.get("v.ProductCategory"));
            }
        });
        $A.enqueueAction(action);
		
	}*/
})